[pytest]
minversion = 7.0
testpaths = backend/tests
pythonpath = . backend
addopts = -ra -q --strict-markers
asyncio_mode = auto
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning